export default class CannedPolicy {
    #private;
    url: string;
    expireTime: number;
    ipRange: string;
    constructor(url: string, expireTime: number, ipRange?: string);
    toJSON(): string;
}
